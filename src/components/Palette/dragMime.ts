/**
 * Mime type used when dragging a symbol from the palette onto the canvas.
 * Centralised so the producer (palette item) and consumer (canvas drop) stay
 * in sync.
 */
export const DRAG_DATA_TYPE = "application/x-pid-symbol";
